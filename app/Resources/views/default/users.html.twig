{% extends 'base.html.twig' %}

{% block body %}
    <h1>Create Users</h1>
    {{form_start(form)}}
    {{form_widget(form)}}
    <button type="submit">Crea Utente</button>
    {{form_end(form)}}
{% endblock %}